module.exports = {
  name: "password_resets",

  run: async (db) => {
    console.log("⏭️ Skip password_resets seed");
    console.log("   Password reset dibuat saat user meminta reset password.");
  },
};
